# URL del backend FastAPI. En dev local típicamente http://localhost:8000.
# En docker compose el frontend habla con `http://backend:8000` interno (no aplica acá — esto es para el navegador).
VITE_API_URL=http://localhost:8000
